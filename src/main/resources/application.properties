spring.application.name=TransitWeb

# =============================
# Datasource MySQL
# =============================
spring.datasource.url=jdbc:mysql://localhost:3306/transitweb?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# =============================
# JPA / Hibernate
# =============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# =============================
# CORS (si usas frontend separado)
# =============================
# Puedes gestionar CORS en SecurityConfig; estas son alternativas

# =============================
# JWT (clave de ejemplo; cámbiala en prod y usa env vars)
# =============================
app.jwt.secret=CAMBIA_ESTA_SECRETA_SUPER_LARGA
app.jwt.expiration-ms=86400000
